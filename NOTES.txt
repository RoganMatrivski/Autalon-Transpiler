fn arguments should include optional
type should have a case?
use Rust Enum matching to minimalize errors like unimplemented functions
discard inserting function snippet, and just replace functions name, and replace args like $1, $2, etc
add inexact version of input by label

On function list change, edit these:
    builtin_pkg_list
    pkg_fn_checker
    katalon_prealpha